# 0G Compute Router API key (get one at https://pc.0g.ai). Fund the Router balance in "Router" mode.
ZOG_API_KEY=

# Optional: use the testnet router instead of mainnet
# ZOG_BASE_URL=https://router-api-testnet.integratenetwork.work/v1
